# Point and Figure Chart with MetaTrader 5

Web application สำหรับสร้าง Point and Figure Chart จากข้อมูลที่ดึงมาจาก MetaTrader 5 (MT5)

## ✨ Features

- 📊 **Point and Figure Chart**: แสดงกราฟแบบ Point and Figure แบบ real-time
- 🔗 **MT5 Integration**: เชื่อมต่อกับ MetaTrader 5 เพื่อดึงข้อมูลราคา
- 📱 **Responsive Design**: รองรับทั้ง Desktop และ Mobile
- ⚙️ **Customizable**: ตั้งค่า Box Size และ Reversal Amount ได้
- 🎨 **Beautiful UI**: Interface ที่สวยงามและใช้งานง่าย

## 🛠️ Installation

### ข้อกำหนดเบื้องต้น

1. **Python 3.8+**
2. **MetaTrader 5** ติดตั้งบนเครื่อง Windows
3. **MT5 Account** (Demo หรือ Live)

### ขั้นตอนการติดตั้ง

1. **Clone หรือ Download โปรเจค**

2. **ติดตั้ง Dependencies**
```bash
cd pnf_mt5_project
pip install -r requirements.txt
```

3. **แก้ไขไฟล์ config.json**
```json
{
  "mt5": {
    "path": "C:/Program Files/MetaTrader 5/terminal64.exe",
    "login": 12345678,
    "password": "your_password",
    "server": "YourBroker-Demo"
  },
  "pnf": {
    "box_size": 10,
    "reversal_amount": 3,
    "method": "traditional"
  },
  "api": {
    "host": "0.0.0.0",
    "port": 8000
  }
}
```

**หมายเหตุ:**
- `path`: ตำแหน่งไฟล์ MT5 terminal64.exe
- `login`: หมายเลขบัญชี MT5 ของคุณ
- `password`: รหัสผ่านบัญชี MT5
- `server`: ชื่อ Server ของ Broker

## 🚀 Running the Application

### วิธีที่ 1: รันด้วย Python โดยตรง

```bash
python main.py
```

### วิธีที่ 2: รันด้วย Uvicorn

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

เมื่อ Server รัน สามารถเข้าใช้งานได้ที่:
- **Web Browser**: http://localhost:8000
- **Mobile**: http://[your-computer-ip]:8000

## 📖 Usage Guide

### 1. เชื่อมต่อ MT5

1. เปิด MetaTrader 5 บนเครื่อง Windows
2. ในหน้า Web Application กรอก:
   - **MT5 Login**: หมายเลขบัญชี
   - **Password**: รหัสผ่าน
   - **Server**: ชื่อ Server
   - **MT5 Path**: (Optional) ถ้าไม่ได้ใช้ path มาตรฐาน
3. คลิก **"เชื่อมต่อ MT5"**

### 2. โหลดกราฟ

1. กรอกข้อมูล:
   - **Symbol**: คู่เงินที่ต้องการ (เช่น EURUSD, GBPUSD)
   - **Timeframe**: กรอบเวลา (M1, M5, M15, M30, H1, H4, D1)
   - **Bars**: จำนวน Bars ที่ต้องการวิเคราะห์
   - **Box Size**: ขนาดของแต่ละ Box (หน่วยเป็น pips)
   - **Reversal Amount**: จำนวน Box ที่ต้องกลับตัว
2. คลิก **"โหลดกราฟ"**

### 3. อ่านกราฟ

- **X (สีเขียว)**: แนวโน้มขึ้น (Bullish)
- **O (สีแดง)**: แนวโน้มลง (Bearish)
- แต่ละคอลัมน์แทนการเคลื่อนไหวของราคาในทิศทางเดียว
- เมื่อราคากลับตัวตาม Reversal Amount จะเปลี่ยนคอลัมน์

## 📁 Project Structure

```
pnf_mt5_project/
├── main.py                 # FastAPI application
├── mt5_connector.py        # MT5 connection handler
├── pnf_calculator.py       # Point and Figure calculator
├── config.json             # Configuration file
├── requirements.txt        # Python dependencies
└── static/
    └── index.html          # Frontend UI
```

## 🔧 API Endpoints

### Connection
- `POST /api/connect` - เชื่อมต่อ MT5
- `GET /api/disconnect` - ตัดการเชื่อมต่อ
- `GET /api/status` - ตรวจสอบสถานะการเชื่อมต่อ

### Data
- `GET /api/symbols` - ดึงรายชื่อ Symbol ทั้งหมด
- `GET /api/symbol/{symbol}` - ดึงข้อมูล Symbol
- `POST /api/chart` - ดึงข้อมูล Point and Figure Chart

### Configuration
- `GET /api/config` - ดึงการตั้งค่าปัจจุบัน

## 📊 Point and Figure Chart คืออะไร?

Point and Figure (P&F) Chart เป็นกราฟที่:
- ไม่คำนึงถึงเวลา มองเฉพาะการเคลื่อนไหวของราคา
- แสดงเฉพาะการเปลี่ยนแปลงที่มีนัยสำคัญ
- กรองสัญญาณรบกวน (Noise) ออก
- ช่วยระบุ Trend และ Support/Resistance ได้ชัดเจน

### การตั้งค่า

**Box Size**: ขนาดของการเคลื่อนไหวขั้นต่ำที่จะบันทึก
- เล็ก (5-10): ละเอียด ใช้สำหรับ Scalping
- กลาง (10-50): ใช้สำหรับ Day Trading
- ใหญ่ (50-100): ใช้สำหรับ Swing Trading

**Reversal Amount**: จำนวน Box ที่ต้องกลับตัวเพื่อเปลี่ยนทิศทาง
- น้อย (2-3): ไวต่อการกลับตัว
- มาก (4-5): กรองสัญญาณเท็จได้ดีขึ้น

## ⚠️ Troubleshooting

### ปัญหา: เชื่อมต่อ MT5 ไม่ได้
- ตรวจสอบว่า MT5 เปิดอยู่
- ตรวจสอบ Login, Password, Server ให้ถูกต้อง
- ลอง Restart MT5

### ปัญหา: ไม่มีข้อมูล Symbol
- ตรวจสอบว่า Symbol มีใน MT5 Market Watch
- ลองเปิด Chart ของ Symbol นั้นใน MT5 ก่อน

### ปัญหา: กราฟไม่แสดงผล
- ตรวจสอบว่ามี Bars เพียงพอ
- ลองปรับ Box Size ให้เหมาะสม
- ลองใช้ Symbol และ Timeframe อื่น

## 🔐 Security Notes

- **อย่า commit ไฟล์ config.json** ที่มีรหัสผ่านจริง
- ใช้บัญชี Demo สำหรับการทดสอบ
- ตั้งค่า CORS และ Authentication สำหรับ Production

## 📝 License

MIT License - ใช้งานได้อย่างอิสระ

## 👨‍💻 Developer Notes

### การพัฒนาต่อยอด

1. **เพิ่ม Authentication**: ใช้ JWT หรือ OAuth2
2. **WebSocket**: Real-time updates
3. **Database**: เก็บประวัติการวิเคราะห์
4. **Technical Indicators**: เพิ่มตัวชี้วัดทางเทคนิค
5. **Alert System**: แจ้งเตือนเมื่อเกิด Pattern สำคัญ

### การ Deploy

**Heroku:**
```bash
heroku create your-app-name
git push heroku main
```

**Docker:**
```dockerfile
FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 📞 Support

หากมีปัญหาหรือคำถาม:
1. ตรวจสอบ README นี้อีกครั้ง
2. ดู API Documentation ที่ http://localhost:8000/docs
3. ตรวจสอบ MT5 logs

---

**Happy Trading! 📈💰**
